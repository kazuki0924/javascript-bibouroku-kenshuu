/**
 * * 「ある条件が成り立つ時だけある処理を行う」プログラムを条件分岐と言います。
 * if文を用いると「もし A ならば B を行う」という条件分岐が可能になります。
 */

const level = 12;

// 条件式を「level > 10」とするif文を作ってください
if (level > 10) {
  console.log('レベルが10より大きいです');
}