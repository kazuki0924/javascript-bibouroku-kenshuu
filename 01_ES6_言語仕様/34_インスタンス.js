/**
 * クラスからオブジェクトを生成するには「new クラス名()」とします。
 * * クラスから作成したオブジェクトは特別にインスタンスと呼びます。
 * e.g. AnimalクラスのインスタンスをAnimalインスタンスと呼びます。
 */

class Animal {
}

// Animalクラスのインスタンスを定数animalに代入してください
const animal = new Animal();

// 定数animalの値を出力してください
console.log(animal);